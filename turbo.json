{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["dist/**"],
      "cache": false
    }
  },
  "globalDependencies": [
    "tsconfig.json"
  ]
}
